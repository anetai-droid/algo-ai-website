# Algo AI サイト 公開・本番設定チェックリスト

最終更新: 2026-06-10 / 対象: `C:\ANKEN\Argo-AI`（静的Vite + Docker）

このサイトは「未設定でも壊れない」設計です。下記の値は **`.env`（および一部はサーバー側）に設定された場合のみ表示・動作** します。推測値は入れず、確定後に設定してください。

---

## A. 公開・本番設定チェックリスト

### A-1. `.env` に設定する値（ビルド前）
| 変数 | 用途 | 未設定時の挙動 | 状態 |
|---|---|---|---|
| `SITE_URL` | 本番URL（canonical/OGP/sitemap） | 仮 `https://algo-ai.jp` を使用 | ⬜ 正式ドメイン確定後に設定 |
| `VITE_GA4_ID` | GA4測定ID | GAタグ非出力 | ⬜ 取得後に設定 |
| `VITE_HOTJAR_ID` | HotjarサイトID | Hotjar非出力 | ⬜ 取得後に設定 |
| `VITE_LINE_URL` | LINE公式 友だち追加URL | LINE導線・文言・連絡方法を全非表示 | ⬜ 公式アカウント開設後に設定 |
| `VITE_BOOKING_URL` | 予約URL（Calendly等） | 予約ボタン非表示（フォーム導線にフォールバック） | ⬜ ツール選定後に設定 |
| `VITE_TEL` | 電話番号 | 電話導線（contact/footer）非表示 | ⬜ 番号確定後に設定 |
| `VITE_CONTACT_ENDPOINT` | フォーム送信先 | 既定 `/api/contact.php` | ✅ 既定でOK（外部フォーム利用時のみ変更） |

> 設定後は必ず再ビルド（`docker compose run --rm build`）。値はビルド時に埋め込まれます。

### A-2. サーバー側設定（デプロイ時）
- ⬜ `public/api/config.php` を `config.sample.php` から作成し、送信先メール（`admin_email`）・`from_email` を設定（**Git管理外**）
- ⬜ レンタルサーバーで PHP `mb_send_mail` が有効か確認、フォーム実送信テスト（管理者通知＋自動返信＋thanks遷移）
- ⬜ HTTPS/SSL 有効化、`http→https` リダイレクト
- ⬜ 独自ドメイン割当、`www` 有無の正規化

### A-3. 公開前の差し替え物
- ⬜ `public/robots.txt` / `public/sitemap.xml` のドメインを正式URLへ（現状 `algo-ai.jp` 仮）
- ⬜ `public/images/ogp.png`（現状ブランド生成版）を正式デザインに差し替える場合は1200×630で
- ⬜ favicon（`public/favicon.svg`）を正式ロゴへ（必要なら）
- ⬜ プライバシーポリシー制定日（現状「公開時に確定」）→ 公開日を記入

### A-4. 計測・解析（要件7-4）
- ⬜ GA4 設定 + コンバージョン設定（問い合わせ送信 `contact_submit` / LINE遷移 `line_click` / 予約 `floating_booking`・`contact_booking` を計測済イベントとして登録）
- ⬜ Google Search Console 登録 + sitemap送信
- ⬜ Hotjar 設置確認

### A-5. ビルド/品質
- ✅ Dockerビルド成功・内部リンク切れ0・PC/SP目視（Phase 1-3で実施）
- ⬜ 公開後 PageSpeed Insights 実測（要件目標 PC90 / SP80）
- ⬜ 構造化データテスト（Organization/Service/FAQPage/HowTo/ItemList/BlogPosting）

---

## B. 依頼主への確認事項一覧（推測不可・未確定）

> いずれも資料間で不一致または未記載のため、**最新の登記・名刺・本人確認で確定**が必要です。確定するまで本サイトには反映していません。

### B-1. 会社情報（資料間で矛盾）
| 項目 | 資料にある値 | 現サイト表示 | 要確認 |
|---|---|---|---|
| 所在地 | 江戸川区北小岩 / 新宿区大久保2-7-1 / さいたま市見沼区 | 江戸川区北小岩5-30-14（暫定） | ⬜ 登記・名刺で確定 |
| 電話番号 | 03-5809-2771 / 090-2449-3321 | 会社概要に 03-5809-2771（暫定） | ⬜ 公開用番号を確定（`VITE_TEL`にも設定） |
| 代表社員名 | 澤田 美由紀 / 沢田 美由紀（表記揺れ） | 「澤田」（本文のみ） | ⬜ 正式表記を確定 |
| 古物商許可番号 | 431060052580 | 表示中 | ⬜ 公開可否確認 |
| 関連会社 | 株式会社FiveStorm | 未掲載 | ⬜ 掲載要否 |

### B-2. ブランド・ドメイン
- ⬜ 正式ドメイン（第一候補 `algo-ai.jp` / 第二 `algo-consulting.jp`）の取得・確定、`algo-ai.com` 同時取得（要件2-2）
- ⬜ サービス名「Algo AI」の正式決定・商標確認（要件11-1）

### B-3. 連絡・予約チャネル
- ⬜ LINE公式アカウント開設 → 友だち追加URL（要件 最優先事項）
- ⬜ 送信先メール（候補 `info@algo11235.com`）の受信可否確認
- ⬜ 予約ツール（Calendly / Googleカレンダー）の選定・URL発行

### B-4. 掲載コンテンツ
- ⬜ **開発実績8件の公開可否**（野菜出荷数予測/競馬予想/ネパール語学習/AI経理/数学問題集自動生成/HP脆弱性診断/学会論文チェック/HP制作）。顧客名・成果数値・顧客の声は未掲載のまま。公開可なら可、成果数値を出す場合は実数の提供が必要
- ⬜ 料金：本サイトは金額を出さず「個別見積もり／固定費用型／パッケージ＋カスタマイズ／時間単価制または成果物単位」と表記（要件4-3準拠）。金額レンジを公開する方針なら確定値の提供が必要
- ⬜ **代表プロフィール（Person構造化データ）は保留中**：氏名表記の確定 + 公開してよい経歴・保有資格・登壇/メディア掲載（要件6-2 E-E-A-T）が揃えば実装

### B-5. グループ連携（要件9-2）
- ⬜ 相互リンク先（digital-legacy.jp ほか）の公開URL確定後にフッター等へ追加

---

### 凡例
✅=対応済 / ⬜=未対応（公開前 or 依頼主確認待ち）
